import React from "react";
import { header } from "./styles.module.scss";

export default function Header() {
  return (
    <header>
      HEADER
      <div className={header}>SVGi</div>
    </header>
  );
}
